name: maple_alerts
description: "Canadian financial deadlines — RRSP, TFSA, GIC, mortgage reminders"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.0
  cloud_firestore: ^5.6.0
  firebase_messaging: ^15.2.0
  flutter_local_notifications: ^18.0.0
  timezone: ^0.10.0
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  sqflite: ^2.4.2
  path: ^1.9.0
  shared_preferences: ^2.3.4
  purchases_flutter: ^8.0.0
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1
  percent_indicator: ^4.2.3
  intl: ^0.20.1
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  in_app_review: ^2.0.9
  uuid: ^4.5.1
  http: ^1.2.0
  aptabase_flutter: ^0.4.1
  html: ^0.15.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
